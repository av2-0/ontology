# ============================================
# Запрос 1: Найти все рукописи (Manuscript)
# Онтология: Единое информационное пространство архива А.А. Вознесенского
# ============================================
# Находит все экземпляры класса Manuscript (Рукопись)
# Через метку класса
# ============================================

PREFIX rdf: <http://www.w3.org/1999/02/22-rdf-syntax-ns#>
PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
PREFIX onto: <http://webprotege.stanford.edu/>

SELECT ?рукопись ?labelРукописи
WHERE {
  ?рукопись rdf:type ?класс .
  ?класс rdfs:label "Manuscript" .
  OPTIONAL { ?рукопись rdfs:label ?labelРукописи }
}
ORDER BY ?рукопись
